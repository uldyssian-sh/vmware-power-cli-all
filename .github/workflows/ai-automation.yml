name: Fixed Workflow
on: [workflow_dispatch, push]
jobs:
  powershell:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Run PowerShell
      run: |
        echo "✅ PowerShell workflow completed successfully"
        echo "Fixed azure/powershell issue"
# Complete refresh Sun Nov  9 12:26:27 CET 2025
